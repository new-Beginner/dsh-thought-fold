# dsh-thought-fold 2.1.0 验证记录

## 交付范围

从项目 2.0.0 基线实现：每个整轮过程组的底部收起、当前阅读组的唯一浮动收起、旧会话兼容默认折叠。原生与兼容模式共享入口和状态迁移规则，单独思考块保留原生顶部控件。

未替换已安装插件、未修改 DSH 宿主组成/源码/通用设置、未部署到当前 GUI、未提交或推送 Git。保留原 `dsh-thought-fold-2.0.0.tgz`。

## 实际执行结果

| 验证 | 结果 |
| --- | --- |
| `npm run build` | 通过；生成客户端 |
| `npm test` | 通过；14 个 node:test 用例、原有客户端检查全部通过 |
| 设置生命周期专项 | 10 个用例、69 次断言通过，已包含在 npm test 中 |
| `npm run test:browser` | 本机无头 Microsoft Edge + React 18，18 个场景通过，0 页面错误 |
| `git diff --check` | 通过；仅 Git 的 LF/CRLF 提示，无补丁空白错误 |
| `npm run pack:plugin` | 通过；打包前自动重新构建 |
| 安装包解包后的构建一致性检查 | 通过 |
| 包内 client.js / 两个客户端源文件与工作区字节比较 | 全部一致 |

## 浏览器场景

1. 旧会话默认折叠，用户消息、最终回答保持可见。
2. 长过程展开时唯一浮动按钮位于输入区上方。
3. 输入框增高后浮动按钮随之上移。
4. 重绘保留选择，多组展开时仅收起当前组。
5. 短过程使用本组底部入口，不出现重复浮动入口。
6. 会话切换、历史延迟补载自动折叠，无旧按钮残留。
7. 运行中不折叠，仅宿主时间线结束更新也能触发折叠。
8. 原生组默认收起，手动展开和重复重绘无重复按钮。
9. 原生浮动收起同步顶部状态。
10. 原生→兼容→原生两种方向保留用户选择。
11. `beforematch` 事件同步展开整个兼容组。
12. 自动收起不抢输入框焦点，不跳动测试中的滚动位置。
13. 430px 窄窗口及恢复宽度后按钮仍在视口内。
14. 切换会话后旧浮动按钮回调不能影响新会话。
15. 单独思考块不注入额外底部控件。
16. 外观开关不影响功能；总开关恢复内容且可重新启用。
17. 空闲帧中不轮询聊天节点、不形成观察器循环。
18. 停用后移除按钮、恢复兼容隐藏，后续 DOM 更新无增强副作用。

浏览器测试运行在独立页面，使用真实 React 渲染模拟宿主提供的节点与状态，结构按本机 Desktop 的打包宿主代码核对。**这不是截图所指历史会话的安装后实测**。`beforematch` 用例直接派发浏览器事件，不等同于自动操作浏览器 Ctrl+F 界面。

## 稳定性修复

独立代码审查后补强了：原生/兼容往返状态迁移、初始化焦点和滚动恢复、可搜索隐藏的属性所有权、设置请求挂载代际与 owner 校验、AbortController/toast 清理、时间线无 DOM 变化触发、过期按钮目标校验。

运行时异常采取恢复内容并停止当前控制器的降级方式；缺少所需 Hook/标记或无法确认已结束过程边界时保持原始显示。没有用文本猜测规则扩大隐藏范围。

## 安装包

- 文件：`dsh-thought-fold-2.1.0.tgz`
- 大小：26,375 字节
- SHA-256：`4a498929d3226ae33d38b0d3bc64abee0cd2626c422164e14ede2e84d73606c1`
- 包内 11 个文件，不包含宿主参考提取物、浏览器测试依赖或测试缓存。

## 安装后验收建议

安装新包并重启 DSH 后，打开用户截图中的旧会话：

1. 确认过程默认收起、最终回答仍可见。
2. 展开长过程，滚动到中段，确认输入区上方出现收起按钮。
3. 同时展开两段，分别滚动查看并收起，确认不会关闭另一段。
4. 增高输入框，再滚动到过程末尾，确认浮动与底部入口正确交接。
5. 切换会话再返回，确认初始化正常。

若目标旧会话缺失可识别的轮次/最终回答元数据，插件会故意保持可见；需针对其实际结构继续核对，而不是承诺所有未知历史格式都能自动折叠。
